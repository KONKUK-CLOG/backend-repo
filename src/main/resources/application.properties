# 공통 — 프로파일별 값은 application-{profile}.properties 참고
spring.application.name=clog

# 기본은 dev (로컬). 운영: SPRING_PROFILES_ACTIVE=prod 필수
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

server.port=${SERVER_PORT:8080}
